import { Routes, Route, Outlet, Link } from "react-router-dom";

export default function Login() {
    return (
        <div>
        Login page
        
        <Outlet />
        </div>
        
    );
}